const ApiError = require("../error/ApiError");
const Route = require("./Route");

class CheckoutRoute extends Route {
  constructor() {
    super();
  }

  // TODO: Créer une méthode qui permet de récupérer le panier, chercher les products en bdd et appeler le service Stripe
}
